classdef GenericPF
    % general Place field class
    
    properties
        
        datsetType;
        filebase;
        % place field parameter structure
        % subfields :
        pfPars;
 com;
        %         smoothRateMap;
        %         selectedPairs;
        %         stablePFUnits;
        %         stablePairs;
        %         spatialCoherence;
        %         sparsity;
    end
    
    
    methods
        
        function genericPF = GenericPF(arg, varargin)
            % class constructor
            
            [filebase] = DefaultArgs(varargin, {[]});
            
            if isempty(arg)
                genericPF.filebase = [];
            elseif isa(arg, 'GenericTrial')
                genericPF.datasetType = arg.datasetType;
                genericPF.filebase = arg.filebase;
                if FileExists() % load pre computed place field files
                end
            end
            
            
            
        end
        
        function rateMap = ComputeRateMap()
            % 
            
            fprintf('\n computing rate map for unit %d of %d units \n'); 
            
            nClus
            for 
            
        end
        
    end
